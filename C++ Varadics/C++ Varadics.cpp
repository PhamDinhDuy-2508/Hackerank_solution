#include iostream
using namespace std;


int d = -1; 
templatebool...digits 
int  reversed_binary_value() {                
  d++;    
  if (d == 64) {
    d = 0;  
  } 
  return d; 
}

template int n, bool...digits
struct CheckValues {
  	static void check(int x, int y)
  	{
    	CheckValuesn-1, 0, digits...check(x, y);
    	CheckValuesn-1, 1, digits...check(x, y);
  	}
};

template bool...digits
struct CheckValues0, digits... {
  	static void check(int x, int y)
  	{
    	int z = reversed_binary_valuedigits...();
    	stdcout  (z+64y==x);
  	}
};

int main()
{
  	int t; stdcin  t;

  	for (int i=0; i!=t; ++i) {
		int x, y;
    	cin  x  y;
    	CheckValues6check(x, y);
    	cout  n;
  	}
}
